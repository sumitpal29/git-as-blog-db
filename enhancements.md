Allow user to any type of data apart from blog

type - blog / JSON

if JSON choosen
ask file name
input to add user json data, 
give pretify option and validate json
on save pretify and validate json

test git sync
content should be going to
git@github.com:sumitpal29/sumit-pal-portfolio-database.git

current repo where entire project sets
git@github.com:sumitpal29/git-as-blog-db.git


setting page design fixes
- make content center
- add theme ooptions - light (current), dark, solarised
- when user is in setting page hide setting icon from header


ignore .git as folder
option to delete a project folder with confirmation modal